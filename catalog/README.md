# Catalog

This directory is the content and metadata layer for the dataset library.

It is split into:

- `datasets/`: one metadata file per dataset
- `guides/`: analysis and usage guides per dataset
- `articles/`: article content and publishing assets

The first version stays file-based on purpose so it is easy to edit, review,
and grow before moving catalog metadata into the database later.
