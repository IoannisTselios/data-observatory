# Articles

This directory is reserved for article content and publishing assets.

The recommended first format is Markdown with frontmatter so articles stay:

- easy to write
- easy to version
- easy to render in the frontend

This is intentionally empty for now because the current phase focuses on the
dataset library foundation first.
