# Shared Pipeline Helpers

This directory is reserved for reusable ingestion helpers.

Expected future helpers include:

- CKAN API pagination
- CSV loading
- HTTP fetch utilities
- source validation
- ingest logging helpers

It is intentionally light right now so the structure is ready before more
datasets are added.
