# Notebooks

This directory contains exploratory analysis notebooks.

The current traffic notebook remains at the top level for compatibility.
Future dataset notebooks should be organized under:

- `notebooks/datasets/<dataset_key>/`
- `notebooks/templates/`

That will make it easier to scale from one analysis notebook to a real dataset
library workflow.
